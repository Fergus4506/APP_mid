package com.example.mid_work

interface ChartUpdateListener {
    fun updateChart(month: String, year: String)

    companion object {
        fun updateChart(toString: String, toString1: String) {

        }
    }
}